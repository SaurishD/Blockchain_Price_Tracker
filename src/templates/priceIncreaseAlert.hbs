<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crypto Price Alert</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
</head>
<body class="bg-gray-100 font-inter flex items-center justify-center min-h-screen">
    <div class="bg-white shadow-xl rounded-xl p-8 max-w-md w-full mx-4 border-2 border-blue-50">
        <div class="flex items-center justify-between mb-6">
            <div class="flex items-center space-x-4">
                <img src="/api/placeholder/60/60" alt="{{cryptoName}} Logo" class="w-12 h-12 rounded-full">
                <h1 class="text-2xl font-bold text-gray-800">{{cryptoName}} Alert</h1>
            </div>
            <div class="bg-green-100 text-green-800 px-3 py-1 rounded-full text-sm font-semibold">
                Price Increase
            </div>
        </div>

        <div class="bg-blue-50 rounded-lg p-4 mb-6">
            <div class="flex justify-between items-center">
                <div>
                    <p class="text-gray-600 mb-2">Price Increase</p>
                    <h2 class="text-3xl font-bold text-green-600">+{{percentIncrease}}%</h2>
                </div>
                <div class="text-right">
                    <p class="text-gray-600 mb-2">Current Price</p>
                    <h3 class="text-xl font-semibold text-gray-800">${{currentPrice}}</h3>
                </div>
            </div>
        </div>

        <div class="border-t border-gray-200 pt-4">
            <div class="flex justify-between text-sm text-gray-600">
                <p>Time Frame</p>
                <p>Last 1 Hour</p>
            </div>
            <div class="flex justify-between text-sm text-gray-600 mt-2">
                <p>Timestamp</p>
                <p>{{timestamp}}</p>
            </div>
        </div>
    </div>
</body>
</html>